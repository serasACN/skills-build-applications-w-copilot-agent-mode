# OctoFit Frontend (scaffold)

Minimal frontend scaffold. To start the dev server:

```bash
cd octofit-tracker/frontend
npm install
npm run dev
```

This project uses `vite` by default; replace with your preferred setup.
